project.version=0.1.3
project.package=net
project.name=FileSynchronizer
project.vendor=Gabriel DROMARD
project.short.name=dromard.filesynchronizer

home.dir=..
bin.dir=.
src.dir=${home.dir}/src
lib.dir=${home.dir}/lib
class.dir=${home.dir}/classes
jnlp.dir=${home.dir}/bin/jnlp
doc.dir=${home.dir}/doc
javadoc.dir=${doc.dir}/javadoc

codebase=http://gdromard.free.fr/news/jnlp
homepage=http://gdromard.free.fr/

jar.name=${project.short.name}
zip.name=${project.short.name}
main.class=FileSynchronizer

jar.common.name=dromard.common
jar.common.version=1.9

build.optimize=true
build.debug=false
build.verbose=false
